import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 40,
        marginTop: 10,
        marginBottom: 15,
    }
})
export default styles;